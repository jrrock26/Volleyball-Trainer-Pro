name: Upload IPA from Artifact URL to TestFlight

on:
  workflow_dispatch:

jobs:
  upload:
    runs-on: macos-14

    env:
      ASC_API_KEY: ${{ secrets.ASC_API_KEY }}
      ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
      ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Download artifact ZIP directly from URL
        run: |
          curl -L -o artifact.zip \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "https://github.com/jrrock26/Volleyball-Trainer-Pro/actions/runs/25752527196/artifacts/6952379122"

      - name: Extract IPA
        run: |
          unzip artifact.zip
          IPA_FILE=$(ls *.ipa)
          echo "IPA_FILE=$IPA_FILE" >> $GITHUB_ENV

      - name: Install Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.1

      - name: Install Fastlane
        run: sudo gem install fastlane -NV

      - name: Create API key JSON
        run: |
          echo "$ASC_API_KEY" | base64 --decode > AuthKey.p8
          cat <<EOF > api_key.json
          {
            "key_id": "$ASC_KEY_ID",
            "issuer_id": "$ASC_ISSUER_ID",
            "key": "$(cat AuthKey.p8 | sed 's/$/\\n/' | tr -d '\n')"
          }
          EOF

      - name: Upload IPA to TestFlight
        run: |
          fastlane deliver \
            --ipa "$IPA_FILE" \
            --api_key_path api_key.json \
            --skip_screenshots \
            --skip_metadata
