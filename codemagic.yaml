workflows:
  ios-dev-client:
    name: iOS Dev Client (Release, No Signing, IPA)

    instance_type: mac_mini_m2

    environment:
      xcode: latest
      node: latest

    scripts:
      - name: Install JS dependencies
        script: npm install

      - name: Install CocoaPods
        script: |
          cd ios
          pod install --repo-update

      - name: Build iOS Dev Client (Release, forced path, no signing)
        script: |
          cd ios
          xcodebuild \
            -workspace VolleyballTrainerPro.xcworkspace \
            -scheme VolleyballTrainerPro \
            -configuration Release \
            -sdk iphoneos \
            SYMROOT=build \
            CODE_SIGNING_ALLOWED=NO \
            EXCLUDED_ARCHS=arm64e \
            build

      - name: Package IPA (Release, guaranteed path)
        script: |
          cd ios/build/Build/Products/Release-iphoneos
          mkdir Payload
          cp -R VolleyballTrainerPro.app Payload/
          zip -r VolleyballTrainerPro.ipa Payload

    artifacts:
      - ios/build/Build/Products/Release-iphoneos/VolleyballTrainerPro.ipa
