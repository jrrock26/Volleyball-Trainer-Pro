workflows:
  ios-dev-client:
    name: iOS Development Client (Native Build)
    instance_type: mac_mini_m2
    environment:
      xcode: latest
      cocoapods: default
      node: latest
      groups:
        - APP_CREDENTIALS

    scripts:
      - name: Install dependencies
        script: |
          npm install
          cd ios
          pod install || true
          cd ..

      - name: Build iOS Dev Client (Xcode)
        script: |
          xcodebuild \
            -project ios/VolleyballTrainerPro.xcodeproj \
            -scheme VolleyballTrainerPro \
            -configuration Debug \
            -sdk iphoneos \
            -archivePath build/VolleyballTrainerPro.xcarchive \
            archive

      - name: Export IPA
        script: |
          xcodebuild \
            -exportArchive \
            -archivePath build/VolleyballTrainerPro.xcarchive \
            -exportOptionsPlist ios/exportOptionsDev.plist \
            -exportPath build

    artifacts:
      - build/**/*.ipa
      - build/**/*.dSYM.zip

    publishing:
      email:
        recipients:
          - jrrock26@gmail.com
        notify:
          success: true
          failure: true

  ios-testflight:
    name: iOS TestFlight Build (Native Build)
    instance_type: mac_mini_m2
    environment:
      xcode: latest
      cocoapods: default
      node: latest
      groups:
        - APP_CREDENTIALS

    scripts:
      - name: Install dependencies
        script: |
          npm install
          cd ios
          pod install || true
          cd ..

      - name: Build iOS Release (Xcode)
        script: |
          xcodebuild \
            -project ios/VolleyballTrainerPro.xcodeproj \
            -scheme VolleyballTrainerPro \
            -configuration Release \
            -sdk iphoneos \
            -archivePath build/VolleyballTrainerPro.xcarchive \
            archive

      - name: Export IPA
        script: |
          xcodebuild \
            -exportArchive \
            -archivePath build/VolleyballTrainerPro.xcarchive \
            -exportOptionsPlist ios/exportOptionsAppStore.plist \
            -exportPath build

      - name: Upload to TestFlight
        script: |
          xcrun altool --upload-app \
            -f build/VolleyballTrainerPro.ipa \
            -t ios \
            -u "$APP_STORE_CONNECT_USERNAME" \
            -p "$APP_STORE_CONNECT_PASSWORD"

    artifacts:
      - build/**/*.ipa
      - build/**/*.dSYM.zip

    publishing:
      email:
        recipients:
          - jrrock26@gmail.com
        notify:
          success: true
          failure: true
