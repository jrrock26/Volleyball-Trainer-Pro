import UIKit
import ffmpeg_kit

class FrameExtractor {
  func extractFrame(at time: Double, from videoPath: String) -> UIImage? {
    // Placeholder: you can wire this to a real ffmpeg command later
    return nil
  }
}
